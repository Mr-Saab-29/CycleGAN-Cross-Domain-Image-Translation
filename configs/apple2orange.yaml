dataset_name: apple2orange
data_root: datasets
output_root: outputs
checkpoint_root: checkpoints/pytorch
logs_root: logs/pytorch
image_size: 256
batch_size: 1
epochs: 100
lr: 0.0002
beta1: 0.5
beta2: 0.999
lambda_cycle: 10.0
lambda_identity: 5.0
save_every_n_epochs: 5
num_workers: 0
subset_size: null
seed: 42
n_residual_blocks: 9
generator_filters: 64
discriminator_filters: 64
channels: 3
soft_fake_label_range: [0.0, 0.3]
soft_real_label_range: [0.7, 1.2]
resume: true
device: cpu
experiment_name: apple2orange_mlops
tracking_uri: mlruns
tracking_experiment: cyclegan
tracking_enabled: true
replay_buffer_size: 50
sample_count: 4
